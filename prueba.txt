@Master(
    New @variable1,(Num, 15);
    CALL(@Procedure1);
    Values(@variable1,
    Alter(@variable1,SUB, 3););
    PrintValues(@variable1);
);

Proc @Procedure1 (
    New @variable3,(Num, 18);
    Values(@variable3, 456789);
    PrintValues(@variable3, "Hola mundo", @variable3);
);

Proc @Procedure2 (
    New @variable4,(Num, 10);
    PrintValues(@variable4);
    Alter(@variable4, ADD, 10);
    PrintValues(@variable4);
);
