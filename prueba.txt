@Master (
    New @variable1(Num, 15);
    PrintValues(@variable1);
    CALL (@Procedure1);
);

Proc @Procedure1 (
    New @variable1,(Num, 15);
    Values(@variable1, 456789);
    PrintValues(@variable1);

);
