@Master(
    New @variable1,(Num, 15);
    PrintValues("hola");
    CALL (@Procedure1);
);

Proc @Procedure1 (
    New @variable2,(Num, 15);
    Values(@variable2, 456789);
    PrintValues(@variable2);
);
